{
  "name": "daily-escape",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build-og-image.mjs && npm run typecheck && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.functions.json",
    "build:share": "npm run build && node scripts/build-artifact.mjs",
    "db:local": "wrangler d1 execute daily-escape-leaderboard --local -c wrangler.local.toml --file=./migrations/0001_init.sql",
    "db:remote": "wrangler d1 execute daily-escape-leaderboard --remote --file=./migrations/0001_init.sql",
    "dev:api": "npm run build && wrangler pages dev dist --d1=DB=daily-escape-leaderboard --compatibility-date=2026-07-01 --port 8788",
    "cf:login": "wrangler login",
    "cf:whoami": "wrangler whoami",
    "db:tables": "wrangler d1 execute daily-escape-leaderboard --remote --command \"SELECT name FROM sqlite_master WHERE type='table' ORDER BY name\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260729.1",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.9",
    "wrangler": "^4.115.0"
  }
}
